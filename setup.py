from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A health monitoring system with pulse, ECG, temperature, and BP sensors connects to cloud and mobile apps to measure vital signs and send alerts if unsafe values are detected',
    author='washim',
    license='',
)
